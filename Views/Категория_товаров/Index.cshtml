@model PagedList.IPagedList<BuildingShop.Models.Категория_товаров>
@using PagedList.Mvc;

<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@{
    ViewBag.Title = "Категории товаров";
}

<script src="~/Scripts/Custom/NotificationScript.js"></script>
<script src="~/Scripts/Custom/LoaderScript.js"></script>

<div class="loader"></div>

<h2>Категории товаров</h2>

<p>
    @Html.ActionLink("Добавить категорию", "Create", new { }, new { @class = "my-custom-link" })
    <button class="my-custom-link" onclick="window.print();">Печать</button>
</p>


@using (Ajax.BeginForm("CategoryPartialView", "Категория_товаров",
    new AjaxOptions
    {
        HttpMethod = "POST",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "divCategories"
    }, new
    {
        id = "NewTableId"
    }
    ))
{
    <p>
        Поиск: @Html.TextBox("SearchString", ViewBag.CurrentFilter as string, new { @id = "searchInput", @oninput = "performSearch()", @class = "form-control" })
    </p>
}
<div id="divCategories">
    @Html.Action("CategoryPartialView")
    @Html.Raw(ViewBag.Data)
</div>


<script>
        function performSearch() {
    var searchString = $("#searchInput").val();
    if (searchString.length > 0) {
        $.ajax({
            url: '@Url.Action("CategoryPartialView", "Категория_товаров")',
            type: 'POST',
            data: { searchString: searchString },
            success: function (data) {
                $('#divCategories').html(data);
                $('.loader').fadeOut();
            }
        });
    } else {
        $('#divCategories').load('@Url.Action("CategoryPartialView", "Категория_товаров")');
    }
}
</script>
